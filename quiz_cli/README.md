# `option_indexing()` function
  * Made some changes in the option_indexing function, check how the errors can be handled when the function returns `None`
# ADDING A FEATURES
  * When an aswer is inputted print out a line than wait for 0.5 sec before the next line is printed

$ Take note that when there is no value in a dictionary when using `dict.get()` it will normally return `None`
$ Whenever you're importing a file or data in your laptop, make sure you change directory to the directory which
is the direct parent of the file that imports the other file
$ To change directory to default cwd, use `cd ~`
