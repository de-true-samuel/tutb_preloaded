# QUESTIONS
- Does the yield statement in a function make a generator?

# PRO TIPS
$ When you want to print the same message for distinct errors in try...except, you can group the errors in a parenthesis, just like this: `except (FileNotFoundError, json.JSONDecodeError):`

# NEW CONCEPTS
- generators
- Paginations
- Classes and Modularization
- uuid module in python
- `default` argument in the `max()` function
- 